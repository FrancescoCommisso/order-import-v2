import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;

  margin-top: var(--component-margin);
  button {
    // height: 70px;
  }
  textarea {
    min-height: 10vh;
    width: 50vw;
  }
`;
